<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>For Muhammad Islam 💖</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <style>
    body {
      margin: 0;
      height: 100vh;
      background: linear-gradient(135deg, #ff9a9e, #fad0c4);
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: 'Comic Sans MS', cursive;
    }

    .card {
      background: white;
      padding: 35px;
      border-radius: 25px;
      text-align: center;
      box-shadow: 0 20px 40px rgba(0,0,0,0.25);
      max-width: 360px;
      animation: pop 0.8s ease;
    }

    @keyframes pop {
      from { transform: scale(0.6); opacity: 0; }
      to { transform: scale(1); opacity: 1; }
    }

    h1 {
      color: #ff4d6d;
      margin-bottom: 10px;
    }

    h2 {
      color: #ff4d6d;
      margin-top: 10px;
    }

    p {
      color: #444;
      font-size: 17px;
    }

    .buttons {
      margin-top: 25px;
      position: relative;
      height: 80px;
    }

    button {
      padding: 12px 25px;
      font-size: 18px;
      border: none;
      border-radius: 20px;
      cursor: pointer;
      position: absolute;
      transition: 0.3s;
    }

    #yes {
      background: #ff4d6d;
      color: white;
      left: 30px;
    }

    #yes:hover {
      transform: scale(1.1);
    }

    #no {
      background: #ccc;
      color: #333;
      right: 30px;
    }

    .celebrate {
      display: none;
      margin-top: 20px;
      font-size: 18px;
      color: #ff4d6d;
      animation: fadeIn 1s ease forwards;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(15px); }
      to { opacity: 1; transform: translateY(0); }
    }

    a.song {
      display: inline-block;
      margin-top: 15px;
      text-decoration: none;
      color: white;
      background: #ff4d6d;
      padding: 10px 18px;
      border-radius: 20px;
      font-size: 16px;
    }
  </style>
</head>

<body>

  <div class="card">
    <p style="font-size:28px; margin:0;">💖 💕 💖</p>

    <h1>Hey Muhammad Islam 💖</h1>
    <p>I was thinking about us…</p>
    <h2>Will you be my Valentine? 💘</h2>

    <div class="buttons">
      <button id="yes">Yes 💖</button>
      <button id="no">No 🙈</button>
    </div>

    <div class="celebrate" id="celebrate">
      YAYYYY 🥹💃  
      <br><br>
      I can’t wait to spend Valentine’s Day with you 💕  
      <br><br>

      <a class="song" target="_blank"
        href="https://open.spotify.com/search/Somewhere%20Only%20We%20Know%20Keane">
        ▶ Play our song 🎶
      </a>
    </div>
  </div>

  <script>
    const noBtn = document.getElementById("no");
    const yesBtn = document.getElementById("yes");
    const celebrate = document.getElementById("celebrate");

    noBtn.addEventListener("mouseover", () => {
      const x = Math.random() * 200 - 100;
      const y = Math.random() * 200 - 100;
      noBtn.style.transform = `translate(${x}px, ${y}px)`;
    });

    yesBtn.addEventListener("click", () => {
      document.querySelector(".buttons").style.display = "none";
      celebrate.style.display = "block";
    });
  </script>

</body>
</html>